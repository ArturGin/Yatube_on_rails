<p id="notice"><%= notice %></p>

<div class="container">
    <div class="card">
        <h1 class="card-header">Latest Posts</h1>
    </div>
</div>

    <%= render 'post_item', post: @posts %>


<%= paginate @posts%>

